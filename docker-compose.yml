version: '3'
services:
  api:
    build: .
    container_name: "transformer_api"
    volumes:
      - ./app/:/usr/src/application/transformer/app:ro
    ports:
      - "8000:15400"
    command: uvicorn app.main:app --host 0.0.0.0 --port 15400 --reload